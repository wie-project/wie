# Lane S2 — background-compile stall elimination (retry)

Scope: ONLY `crates/wie-cpu/src/jit/config.rs` and `crates/wie-cpu/src/jit/lower/mod.rs` (+lower/* if essential).
pipeline.rs is owned by another lane — READ-ONLY for us.

Goal: kill background-compile stalls (bg_stalls=1845, bg_stall_us≈18.6s on critical path;
compiles=1819 vs bg_compiles=7225 anomaly; skip=75; never=144).

## Log

- [start] Read config.rs fully. Knobs understood: BG_QUEUE_CAP=1024, hotness_threshold=100
  (WIE_JIT_HOTNESS_THRESHOLD), pure_loop_hotness=8, eager_block_insns=48,
  bg_wait_timeout=10ms (WIE_JIT_BG_TIMEOUT_US), bg_enabled default on.
